#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir//usr/local/bin/node" ]; then
  "$basedir//usr/local/bin/node"  "$basedir/../imgs/index.js" "$@"
  ret=$?
else 
  /usr/local/bin/node  "$basedir/../imgs/index.js" "$@"
  ret=$?
fi
exit $ret
